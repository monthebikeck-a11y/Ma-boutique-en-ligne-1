// Frontend configuration.
// Keep CinetPay secrets ONLY on the backend.
const STORE_CONFIG = {
  currency: "XAF",
  cinetpay: {
    enabled: true,
    apiEndpoint: "https://TON-BACKEND.com/api/create-payment"
  }
};
